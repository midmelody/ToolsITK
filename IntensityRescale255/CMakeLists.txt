PROJECT(IntensityRescale255)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8) 

FIND_PACKAGE(ITK)
IF(ITK_FOUND)
  INCLUDE(${ITK_USE_FILE})
ENDIF(ITK_FOUND)

ADD_EXECUTABLE(IntensityRescale255 main.cxx)
TARGET_LINK_LIBRARIES(IntensityRescale255 ${ITK_LIBRARIES})
