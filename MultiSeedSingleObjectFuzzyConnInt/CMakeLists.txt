PROJECT(MSSOFuzzyConnSeg3DInt)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8) 

FIND_PACKAGE(ITK)
IF(ITK_FOUND)
  INCLUDE(${ITK_USE_FILE})
ENDIF(ITK_FOUND)

ADD_EXECUTABLE(MSSOFuzzyConnSeg3DInt main.cxx)
TARGET_LINK_LIBRARIES(MSSOFuzzyConnSeg3DInt ${ITK_LIBRARIES})
