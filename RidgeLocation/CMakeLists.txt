PROJECT(RidgeLocation)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8) 

FIND_PACKAGE(ITK)
IF(ITK_FOUND)
  INCLUDE(${ITK_USE_FILE})
ENDIF(ITK_FOUND)

ADD_EXECUTABLE(RidgeLocation main.cxx)
TARGET_LINK_LIBRARIES(RidgeLocation ${ITK_LIBRARIES})
