PROJECT(RGBtoIntensity)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8) 

FIND_PACKAGE(ITK)
IF(ITK_FOUND)
  INCLUDE(${ITK_USE_FILE})
ENDIF(ITK_FOUND)

ADD_EXECUTABLE(RGBtoIntensity main.cxx)
TARGET_LINK_LIBRARIES(RGBtoIntensity ${ITK_LIBRARIES})
