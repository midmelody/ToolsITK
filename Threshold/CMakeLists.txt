PROJECT(Threshold)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8) 

FIND_PACKAGE(ITK)
IF(ITK_FOUND)
  INCLUDE(${ITK_USE_FILE})
ENDIF(ITK_FOUND)

ADD_EXECUTABLE(Threshold main.cxx)
TARGET_LINK_LIBRARIES(Threshold ${ITK_LIBRARIES})
